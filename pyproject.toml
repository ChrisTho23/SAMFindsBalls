[tool.poetry]
name = "samfindsballs"
version = "0.1.0"
description = ""
authors = ["ChrisTho23"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
